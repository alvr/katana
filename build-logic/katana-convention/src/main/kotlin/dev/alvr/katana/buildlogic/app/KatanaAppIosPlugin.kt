package dev.alvr.katana.buildlogic.app

import org.gradle.api.Plugin
import org.gradle.api.Project

internal class KatanaAppIosPlugin : Plugin<Project> {
    override fun apply(target: Project) = with(target) {
    }
}
